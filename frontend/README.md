# Emmas Orca Frontend

## Run locally

```bash
npm install
npm run dev
```

Set `VITE_API_BASE` if your backend is not on `http://localhost:8000/api` (include the `/api` path prefix).

On first visit, the app shows a welcome screen until a friend orca is saved once in the database (same choice on all devices).
